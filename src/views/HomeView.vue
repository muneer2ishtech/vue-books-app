<template>
  <div class="bg-panel border border-app rounded p-4">
    <h1 class="text-xl font-semibold mb-2">{{ $t('appTitle') }}</h1>
    <p class="text-muted">{{ $t('homeDescription') }}</p>
  </div>
</template>
